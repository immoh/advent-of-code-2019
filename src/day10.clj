(ns day10
  (:require
    [clojure.string]))

(defn parse-input [input]
  (map-indexed (fn [y row]
                 (map-indexed (fn [y c] )
                              row
                              )

                 ) (clojure.string/split-lines input)))


(defn part1 [input])